import React from 'react';
import { View, Text } from 'react-native';

const Button = () => {
    return (
        <View>
            <Text>Hello, World!</Text>
        </View>
    );
};

export default Button;